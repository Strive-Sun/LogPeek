## 1. 后端后台索引与进度
- [x] 1.1 `open_log_session` 改为启动后台索引任务并立即返回 `session_id`
- [x] 1.2 建索引期间周期性 emit `index-progress`(session_id、已索引行数上界、done 标志)
- [x] 1.3 `line_count` 返回当前已索引行数上界(未完成时为进行值)
- [x] 1.4 `read_lines` 支持读取已索引范围;请求超出已索引上界时返回已就绪部分
- [x] 1.5 索引完成/失败/会话关闭时终止后台任务并做收尾

## 2. 前端接线
- [x] 2.1 `subscribeIndexProgress` 订阅真实 `index-progress` 事件
- [x] 2.2 进度条随真实进度更新,完成后隐藏
- [x] 2.3 首屏在索引未完成时呈现已就绪行;滚动前沿领先阅读位置

## 3. 验证
- [x] 3.1 大文件打开不阻塞、首屏即时、进度条可见
- [x] 3.2 边建边读:索引进行中即可读取已就绪行
