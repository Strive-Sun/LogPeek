## 1. 后端目录事件管线

- [x] 1.1 定义顶层普通文件节点与目录变化批次协议，保留绝对路径稳定 id、日志/归档分类和旧/新路径
- [x] 1.2 标准化 notify 的 create/remove/rename/modify/error 事件，按监控目录约 200ms 防抖并去重同一路径变化
- [x] 1.3 将稳定检测移动到有界后台队列，按路径 generation 去重，并使删除/重命名取消旧路径结果
- [x] 1.4 对模糊 rename、watcher error/overflow 和队列压力触发受影响目录兜底重扫
- [x] 1.5 初始扫描与兜底扫描返回顶层全部普通文件，不读取每个未知文件的内容样本来决定其是否存在

## 2. 前端增量同步

- [x] 2.1 在 Tauri 与 mock API 中增加 `directory-changed` 订阅及变化批次类型
- [x] 2.2 实现纯函数目录 reducer，支持 upsert、remove、rename、modify 和 snapshot 替换，并保持稳定排序
- [x] 2.3 `App` 订阅结构事件并局部更新受影响目录，移除新日志事件触发的全目录刷新耦合
- [x] 2.4 保持后缀筛选为纯展示规则，确保隐藏文件持续同步且切换“显示全部”立即呈现完整库存
- [x] 2.5 删除/重命名当前查看文件时，使会话与通知状态安全失效，不残留旧路径视图

## 3. 测试与性能保护

- [x] 3.1 增加后端创建、删除、重命名、修改、模糊事件兜底及稳定检测取消测试
- [x] 3.2 增加事件风暴测试，验证同目录大量重复事件合并为有界批次且不触发逐事件全量扫描
- [x] 3.3 增加前端 reducer 与筛选切换测试，覆盖删除旧节点、rename id 更新和隐藏文件恢复展示
- [ ] 3.4 验证 Windows 与 macOS 原生事件序列，记录按层非递归范围和极端大目录限制
- [x] 3.5 运行前端测试、Prettier、ESLint、构建及 Rust fmt、Clippy、测试

## 4. 文档

- [x] 4.1 在 `CHANGELOG.md` 的 `Unreleased` 章节逐条记录实时同步、删除/重命名修复和事件合并优化

## 5. 懒加载递归目录修正

- [x] 5.1 将目录库存节点扩展为文件与文件夹，并增加受监控根目录约束下的按层扫描命令
- [x] 5.2 文件夹展开时建立非递归 watcher，折叠、删除或重命名时释放该子树 watcher
- [x] 5.3 扩展前端 API 与递归 reducer，使变化批次可定位任意已加载目录
- [x] 5.4 接通目录节点的展开加载、加载状态和卸载清理，区分监控根目录与普通子目录操作
- [x] 5.5 增加目录库存、递归 reducer、懒加载和 watcher 生命周期测试，并重新运行完整检查
