## 1. 搜索页面保活

- [x] 1.1 在应用层增加首次打开后的延迟挂载和隐藏切换，避免返回监控目录时卸载搜索页面
- [x] 1.2 调整搜索页面激活与隐藏时的焦点恢复，确保隐藏页面不拦截交互
- [x] 1.3 保持搜索词、结果、筛选、分页、选中项与滚动位置，避免切页重新调用初始化和查询 API

## 2. 启动生命周期

- [x] 2.1 拆分轻量搜索偏好存储与重型 `FileSearchManager`，保持当前/下次启动状态语义
- [x] 2.2 在搜索管理器构造前发布核心应用状态并恢复监控目录 watcher
- [x] 2.3 搜索启用时通过后台阻塞任务延迟初始化并恢复搜索，搜索关闭时完全跳过管理器构造
- [x] 2.4 让搜索命令按需等待管理器就绪，同时保证偏好与非搜索命令不等待搜索初始化

## 3. 验证

- [x] 3.1 增加首次打开、隐藏、再次显示及只初始化一次的前端回归测试
- [x] 3.2 增加核心状态先就绪、禁用时不构造搜索、启用时延迟初始化及命令等待的 Rust 测试
- [x] 3.3 运行前端测试、构建、Lint、格式检查、Rust 测试、clippy、OpenSpec strict validation 和 diff 检查
