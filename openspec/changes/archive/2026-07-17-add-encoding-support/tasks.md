## 1. 后端编码
- [x] 1.1 在发布首批行偏移前完成有限前缀采样,检测 UTF-8、GB18030、UTF-16LE/BE
- [x] 1.2 依 BOM 正确区分 UTF-16LE 与 UTF-16BE,并从首行展示内容中剥离 BOM
- [x] 1.3 按生效编码建立行偏移:UTF-8/GB18030 使用 ASCII 兼容换行,UTF-16 按对应字节序识别代码单元
- [x] 1.4 在索引进度或会话查询结果中返回检测编码与当前生效编码,供前端更新初始占位值
- [x] 1.5 新增命令 `set_session_encoding(session_id, encoding)`,在后台重建 offsets 并在完成后原子切换编码/索引
- [x] 1.6 为连续编码切换增加 generation/取消保护,旧任务不得覆盖较新的用户选择

## 2. 前端交互
- [x] 2.1 "编码 ▾" 改为可交互下拉,展示当前编码
- [x] 2.2 选择编码后调用后台重建命令,展示处理中状态,完成后清空行缓存并刷新可视区
- [x] 2.3 mock 分支补齐编码接口

## 3. 验证
- [x] 3.1 UTF-8 / GBK / GB18030 / UTF-16LE / UTF-16BE(BOM)样本的行数、换行与正文均正确
- [x] 3.2 自动检测错误时手动切换可纠正乱码,且大文件切换期间 UI 不阻塞
- [x] 3.3 快速连续切换编码时仅最后一次选择生效,不存在旧任务回写
