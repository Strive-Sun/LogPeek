## 1. 契约与回归测试
- [ ] 1.1 明确归档条目读取能力模型(顺序 `Read` / 有界 `Read + Seek`),且读取器生命周期可安全跨后台索引任务
- [ ] 1.2 先补 Stored、Deflate、加密条目、实际字节熔断与取消读取的针对性测试

## 2. 流式读取
- [ ] 2.1 `open_entry` 返回流式解压读取器,删除整条目 `Vec<u8>` 缓冲
- [ ] 2.2 首批解压字节直接进入查看层增量缓存/索引,期间持续产生 `index-progress`
- [ ] 2.3 内存占用与条目大小解耦(固定缓冲分块读取)

## 3. Stored 随机访问
- [ ] 3.1 识别 Stored 条目并暴露受条目起止边界约束的字节 seek 能力
- [ ] 3.2 Deflate 条目维持顺序流语义;随机访问交由查看层临时缓存
- [ ] 3.3 确认按行跳转只使用已建立行索引,不把字节 seek 误当作未知行号定位能力

## 4. 安全边界与生命周期
- [ ] 4.1 实际字节熔断在流式路径下仍生效
- [ ] 4.2 加密条目仍返回明确错误
- [ ] 4.3 会话关闭/LRU 回收可取消后台读取并清理查看层临时缓存

## 5. 验证
- [ ] 5.1 大条目打开后首批内容与进度可见,内存峰值显著低于条目大小
- [ ] 5.2 Stored 条目字节 seek 即时且不越过条目边界;Deflate 顺序读正确
- [ ] 5.3 相关单元测试及现有边建边读测试全部通过
