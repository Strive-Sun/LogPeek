## 1. 建立可重复的启动基线

- [x] 1.1 定义跨 Rust 与前端共享的启动阶段名称和单调时钟起点
- [x] 1.2 记录原生首帧、WebView 创建/导航、React 挂载、可交互首帧、核心状态和后台任务阶段
- [x] 1.3 增加前端到 Rust 的 `interactive-frame` 握手，确保主页面首帧真实完成后才标记启动完成
- [x] 1.4 增加发布构建启动基准脚本，输出十次样本的 P50、P90、最大值和阶段明细
- [x] 1.5 在当前代码上记录 Windows 基线，并记录当前缺少 macOS 实机环境

## 2. 缩短首屏关键路径

- [x] 2.1 在原生首帧后立即请求创建和导航主 WebView，移除无关前置步骤
- [x] 2.2 将托盘、更新检查、缓存维护、watcher 恢复和搜索调度置于首屏关键路径之后
- [x] 2.3 让 React 主页面骨架不等待后端业务状态，轻量目录和配置数据就绪后增量填充
- [x] 2.4 优化原生启动绘制频率，避免 WebView 初始化期间持续高频满窗口软件重绘
- [x] 2.5 根据阶段数据确认初始前端执行与进程装载并非主导耗时，本轮无需额外拆分
- [x] 2.6 使用显式可交互握手完成原生画面与 WebView 的无白屏交接，并提供可诊断的失败兜底

## 3. 回归测试

- [x] 3.1 增加生命周期顺序测试，验证 `shell-ready` 不依赖监控、搜索、更新或维护任务
- [x] 3.2 增加前端测试，验证首屏可交互握手只在 React 提交并完成绘制帧后发送一次
- [x] 3.3 验证目录数据延迟期间主导航和基础操作可用，数据就绪后正确填充
- [x] 3.4 验证搜索关闭和开启两种配置均不改变首屏关键路径
- [x] 3.5 验证启动追踪不在 UI 线程同步写磁盘且不上传远程数据

## 4. 性能与质量验收

- [x] 4.1 Windows 发布版连续十次常规启动至少九次在 3 秒内可交互，且无一次超过 5 秒
- [ ] 4.2 macOS 发布版连续十次常规启动至少九次在 3 秒内可交互，且无一次超过 5 秒
- [ ] 4.3 记录双平台冷启动结果，确认全程无白屏且原生进度反馈立即可见
- [x] 4.4 前端 build、test、lint、format 全部通过
- [x] 4.5 Rust fmt、check、test、clippy 全部通过
- [x] 4.6 `openspec validate optimize-startup-time --strict` 通过
