## 1. 搜索索引基础

- [x] 1.1 实现本地固定卷与用户排除范围配置，规范化并持久化搜索根
- [x] 1.2 对 SQLite FTS5 trigram 与轻量 n-gram 方案做百万路径原型基线，根据包体积、建库吞吐和查询延迟选型
- [x] 1.3 实现带 schema 版本的持久化文件元数据索引、批量 upsert/delete 与安全重建
- [x] 1.4 实现有界、可取消的后台扫描，跳过不可读目录、特殊文件、符号链接与联接点循环
- [x] 1.5 实现索引进度、暂停/恢复、大小统计、清除与重建操作
- [x] 1.6 将首次建库改为可恢复的两阶段批量写入，并用百万路径基线验证相对逐行 FTS 的收益

## 2. 增量一致性与查询

- [x] 2.1 使用原生文件系统事件增量更新已索引范围，对事件风暴做有界合并与去重
- [x] 2.2 实现溢出/丢失事件后的脏范围差异校验，不在 UI 调用链路扫描
- [x] 2.3 实现按文件名与路径的子串查询、多词 AND、类型/后缀过滤、排序与分页
- [x] 2.4 为快速连续输入增加 generation/cancellation，确保旧查询不覆盖新结果
- [x] 2.5 在打开、定位或加入监控前重新校验结果路径，处理已删除或重命名文件

## 3. 搜索界面与文件操作

- [x] 3.1 将顶栏占位符替换为可访问的搜索按钮，支持鼠标、键盘快捷键与 Escape 关闭
- [x] 3.2 实现搜索输入、防抖查询、虚拟化结果列表、加载/部分索引/空结果/错误状态
- [x] 3.3 结果展示文件名、完整路径、类型、大小与修改时间，并提供命中文本高亮
- [x] 3.4 双击可识别裸日志时复用现有会话打开预览，双击支持归档时进入归档浏览链路
- [x] 3.5 实现结果右键“将所在目录加入监控”，成功后刷新、展开并定位目标
- [x] 3.6 增加中英文案、键盘导航、焦点恢复与读屏标签

## 4. 验证

- [x] 4.1 单元测试覆盖路径规范化、排除、不跟随链接、增删改事件、溢出恢复、查询排序和取消
- [x] 4.2 前端测试覆盖搜索面板、连续输入、双击打开、右键加入监控和已失效结果
- [x] 4.3 用至少 100 万条合成路径记录建立可重复性能基线，记录建库吞吐、索引大小、空闲资源与首页查询延迟
- [x] 4.4 验证大量文件扫描、索引和查询不阻塞 UI 线程，结果列表内存有界
- [x] 4.5 运行前端 build/lint/test/format 和 Rust fmt/clippy/check/test
- [ ] 4.6 在 Windows 与 macOS 实机验证首次扫描、增量更新、受保护目录、打开日志与加入监控

## 5. Windows NTFS 快速 provider 与服务

- [x] 5.1 抽象搜索 provider 协议与逐卷状态，使 Windows NTFS、兼容目录扫描和后续 macOS provider 共享查询与 UI API
- [x] 5.2 实现按需启动的最小 Windows 索引服务、SCM 生命周期和版本握手，GUI/归档解析保持普通用户权限
- [x] 5.3 实现带本机 ACL、消息长度、批次、并发上限和取消语义的版本化 named-pipe IPC
- [x] 5.4 使用卷句柄与 `FSCTL_ENUM_USN_DATA` 流式解析 MFT 记录，以 file reference/parent reference 重建完整路径
- [x] 5.5 处理卷标识、目录层级、孤儿记录、循环防护、reparse point、排除范围和卷中途离线
- [x] 5.6 将大小、修改时间、可读性和内容类型改为普通用户进程对可见结果的有界按需补齐
- [x] 5.7 在搜索设置和进度区展示逐卷 provider、健康状态、服务安装/修复入口与兼容扫描降级原因

## 6. USN 增量一致性与持久化

- [x] 6.1 为每个卷持久化 volume identity、journal ID、next USN、provider/schema 版本和快照完成标记
- [x] 6.2 使用 `FSCTL_QUERY_USN_JOURNAL` 与 `FSCTL_READ_USN_JOURNAL` 重放创建、删除、重命名和移动变化
- [x] 6.3 在 journal ID 变化、next USN 早于 `FirstUsn`、卷身份变化或记录溢出时只重建受影响卷
- [x] 6.4 实现快照原子切换与崩溃恢复，升级失败时保留上一份完整可查询索引
- [x] 6.5 保留兼容扫描 provider 的 watcher 增量路径，并确保 provider 切换不产生重复、遗漏或陈旧结果

## 7. 安全、安装与性能验收

- [x] 7.1 编写服务威胁模型，验证服务无文件内容读取、网络、任意路径操作或通用提权命令面
- [x] 7.2 用固定二进制夹具测试 MFT/USN V2/V3 解析、边界长度、畸形记录、父引用重建、重命名和 journal gap
- [x] 7.3 对 IPC 解析器执行 fuzz/property 测试，覆盖未认证客户端、超大消息、未知版本、并发风暴与断线取消
- [ ] 7.4 在非管理员 GUI 下做 Windows 集成测试，覆盖服务缺失、安装、启动、停止、升级、协议不兼容和卸载清理
- [ ] 7.5 在至少 1,000,000 个条目的 Windows/NTFS 本地 SSD 参考数据集上验证首批结果 ≤10 秒、完整索引 ≤60 秒、UI 输入 p95 ≤100 毫秒
- [ ] 7.6 验证热启动仅重放 USN、关闭期间变化不丢失，记录与同机 Everything 的时间、峰值内存和索引体积对比
- [ ] 7.7 验证 FAT/exFAT/服务拒绝安装时的显式兼容降级，以及 macOS 现有搜索回归
- [ ] 7.8 更新 MSI/NSIS 安装、修复、升级和卸载配置，验证服务签名与最终安装包仍满足体积约束

## 8. Tantivy 热查询层

- [x] 8.1 引入 MIT 许可的 Tantivy 0.22，建立初版文件名、类型与规范路径唯一键 schema（后续由第 9 节的 Orange GPLv3 架构替换）
- [x] 8.2 将首次 MFT/兼容扫描批次同步写入 Tantivy，并在卷可搜索时 commit/reload，使部分结果持续可见
- [x] 8.3 将 USN、watcher、陈旧结果删除与清空操作同步到 Tantivy，数据库版本变化时安全重建查询索引
- [x] 8.4 查询改用有界 Tantivy 候选与短词/显式路径 SQLite 回退，不在每次输入时执行全库 COUNT（查询语义后续由第 9 节替换）
- [x] 8.5 删除搜索结果表上方逐卷 provider 文本行，保留状态与设置中的诊断入口
- [x] 8.6 增加查询一致性、增量更新、迁移恢复和百万路径性能测试，并运行全量验证

## 9. Orange GPLv3 搜索架构迁移

- [x] 9.1 记录 Orange 来源 commit、GPLv3 衍生关系与发布合规要求
- [x] 9.2 将 Orange 的 schema、文件名切词、简繁/拼音扩展、精确路径和扩展名索引移植到 Tantivy 0.22
- [x] 9.3 将 Orange 的全量 add-only、增量删除、5/2 秒提交和 TopDocs 查询接入 MFT/USN provider
- [x] 9.4 更新查询一致性、中文/拼音、驼峰文件名、类型过滤与迁移恢复测试
- [x] 9.5 运行百万路径、Windows D 盘和全量回归验证

## 10. 多卷查询与进度纠正

- [x] 10.1 将 Orange QueryParser 的默认多词语义从 OR 改为 AND，避免通用扩展名词项占满结果页
- [x] 10.2 对候选结果应用完整文件名、前缀、包含关系和稳定路径排序
- [x] 10.3 分开展示 MFT 已发现记录数与 Tantivy 已可搜索文件数
- [x] 10.4 在运行中的 C/D 双卷真实索引上验证 `debug.log` 同时返回两盘结果
- [x] 10.5 对 LogCrate 与 Orange/Tantivy 相关包启用定向 dev 优化，并复测开发版与 release 首次索引耗时
- [x] 10.6 将查询快照完成标记与 SQLite 持久化状态分离，验证连续热重载不清空完整搜索索引
